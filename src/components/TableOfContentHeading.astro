---
type Heading = {
  depth: number;
  text: string;
  slug: string;
  subheadings: Heading[];
};

export interface Props {
  heading: Heading;
}

const { heading } = Astro.props;
---

<li class="ml-4  marker:dark:text-neutral-500  marker:text-neutral-400">
  <a class=" text-neutral-400 dark:text-neutral-500 hover:text-neutral-500 dark:hover:text-white hover:font-bold" href={"#" + heading.slug}>
    {heading.text}
  </a>
  {
    heading.subheadings.length > 0 && (
      <ul class="list-disc list-outside">
        {heading.subheadings.map((subheading) => (
          <Astro.self heading={subheading} />
        ))}
      </ul>
    )
  }
</li>
