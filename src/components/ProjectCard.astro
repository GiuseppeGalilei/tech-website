---
import type { CollectionEntry } from "astro:content";
import FormattedDate from "./FormattedDate.astro";

export interface Props {
  projects: CollectionEntry<"projects">;
}

const { project } = Astro.props;
---

<a
  href={!project.data.planned ? `/projects/${project.slug}` : null}
  class={`space-y-2 flex flex-col items-start rounded-md p-5 border-2 border-neutral-300 dark:border-neutral-700  bg-neutral-100 hover:bg-neutral-200 dark:bg-neutral-800 dark:hover:bg-neutral-700`}
>
  <div class="space-y-1">
    <div class="flex items-center space-x-2">
      <h2 class="text-lg sm:text-xl font-bold">{project.data.title}</h2>
      {
        project.data.planned && (
          <span class="flex items-center justify-center p-0.5 px-1.5 bg-amber-500 rounded-full text-black font-black text-xs">
            Planned
          </span>
        )
      }
    </div>
    <!-- <div class="flex flex-col">
      <span class="flex items-center space-x-1 text-sm font-bold uppercase">
        <FormattedDate date={project.data.pubDate} />
        {project.data.tags.map((tag) => <span>{` • ${tag}`}</span>)}
      </span>
    </div> -->
  </div>
  <p>{project.data.description}</p>
</a>
